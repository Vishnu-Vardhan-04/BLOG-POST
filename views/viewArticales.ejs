<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Articles</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f7f7f7;
        }
        .container {
            margin-top: 50px;
        }
        .article-card {
            margin-bottom: 20px;
            padding: 20px;
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        h1 {
            color: #333;
        }
        .article-title {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .article-description {
            margin-bottom: 15px;
            color: #555;
        }
        .article-date {
            font-size: 0.85rem;
            color: #777;
        }
        .article-actions {
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Your Articles</h1>
        
        <% if (articles.length === 0) { %>
            <p class="text-muted">You haven't posted any articles yet.</p>
        <% } else { %>
            <% articles.forEach(function(article) { %>
                <div class="article-card">
                    <div class="article-title"><%= article.title %></div>
                    <div class="article-description"><%= article.description %></div>
                    <div class="article-date">Posted on: <%= article.createdAt.toDateString() %></div>
                    
                    <div class="article-actions">
                        <a href="/articles/edit/<%= article._id %>" class="btn btn-warning">Edit</a>
                        <form action="/articles/delete/<%= article._id %>" method="POST" style="display:inline-block;">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
            <% }); %>
        <% } %>
    </div>

</body>
</html>
